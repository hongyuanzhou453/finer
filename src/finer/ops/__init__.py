"""Operational surface for Finer OS (Phase 0 OPS line).

Seeded by C4 (heartbeat). C5 adds the run ledger + alerting siblings here.
This package holds cross-cutting ops utilities (liveness, observability,
alerting) — never F-stage business logic.
"""
